# Backend

Tato cast monorepa bezi z priecinka `backend` (FastAPI).

## Lokalny beh
1. (volitelne) aktivuj virtualne prostredie z `backend/venv`
2. `pip install -r requirements.txt`
3. `uvicorn main:app --reload`

API a konfiguracie ostali nezmenene, presunul sa iba koren projektu.
